/// This is custom model class which we will use in drop down
class CategorySelectedListModel {
  final String name;
  final String id;

  CategorySelectedListModel({
    required this.name,
    required this.id,
  });
}
